# Spec Writer

Brief → Spec: Was gebaut wird, nicht Wie.

## Input
Pfad zum Brief wird beim Aufruf übergeben (z.B. `input/case/brief.md`)

## Invocation
`/spec-writer input/case/brief.md`

## Output
`output/YYYY-MM-DD/spec-eval/spec.md`

## Weiter mit
`/eval-writer` oder direkt `/option-stormer`
